import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gloriana-app',
  webDir: '../../dist/apps/gloriana-app',
  bundledWebRuntime: false,
};

export default config;
